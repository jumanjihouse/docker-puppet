# https://circleci.com/docs/docker
machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker version
    - ss -tulpn
    - script/build.sh

test:
  override:
    - script/test
